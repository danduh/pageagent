{
  "name": "pageagent",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "PageAgent — an on-device Chrome extension that turns any web page into something you can talk to.",
  "license": "MIT",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.node.json",
    "lint": "node -e \"console.log('[pageagent] lint target — eslint + jsx-a11y wired in issue #14.')\"",
    "test": "node -e \"console.log('[pageagent] test target — vitest + axe wired in issue #14.')\""
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.7.1",
    "@types/chrome": "^0.2.2",
    "@types/node": "^22.20.1",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^4.7.0",
    "typescript": "~5.9",
    "vite": "^6.4.3"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  }
}
